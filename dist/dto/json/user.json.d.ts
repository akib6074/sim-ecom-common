import { RoleName } from '../../enum/role-name.enum';
import { Gender } from '../../enum/gender.enum';
export declare const usersObject: {
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    password: string;
    gender: Gender;
    role: RoleName;
    dateOfBirth: Date;
    address: string;
}[];
